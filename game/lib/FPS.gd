extends Label

func _ready():
	set_process(true)

func _process(delta):
	'''
	var game_date = GameState.game_date
	var fps = Engine.get_frames_per_second()
	var memory = OS.get_static_memory_usage() / 1024 / 1024  # Convert to MB
	
	text = "Time: %02d/%02d/%02d %02d:%02d\nFPS: %d\nRAM: %.2f MB" % [\
	game_date["year"], \
	game_date["month"], \
	game_date["day"], \
	game_date["hour"],\
	game_date["minute"], \
	fps, memory]
	'''
